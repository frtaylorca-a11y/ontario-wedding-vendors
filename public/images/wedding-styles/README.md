# Wedding-style reference screenshots

The wizard's Step 1 vibe picker on `/plan/website` shows six reference
designs as visual cards. Drop the JPGs here at these exact paths:

| File                                  | Theme key      | Inspiration              |
| ------------------------------------- | -------------- | ------------------------ |
| `editorial.jpg`                       | `editorial`    | KC Events                |
| `minimal-blush.jpg`                   | `minimal`      | Tanya & Josh             |
| `terracotta.jpg`                      | `terracotta`   | Abigail & Rick           |
| `retro-charm.jpg`                     | `retro`        | Camille & Rowan          |
| `bold-garden.jpg`                     | `bold-garden`  | Giardino                 |
| `frosted-glass.jpg`                   | `frosted`      | J.O. Sullivan            |

**Specs:**
- 16:9 aspect ratio
- ≥ 1200 × 675 px (we serve at up to 600 px wide on desktop, 2×
  for HiDPI)
- JPEG, quality 85, ≤ 200 KB each

Until each file is dropped in, the corresponding card on
`/plan/website` will show a theme-coloured gradient placeholder with
the style name overlaid — the picker still works end-to-end, it just
looks plainer.
